class ProductModel():
    
    def __init__(self, code, name, price_unit, category_id):
        self._code = code
        self._name = name
        self._price_unit = price_unit
        self._category_id = category_id